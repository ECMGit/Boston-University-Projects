1. a. 	Main			TracingMethodCalls
	x	y	|	x	y
	5	2	|	-	-
			|	5	2
			|		7
			|	3		// output: 3 7
		3 	|			// output: 5 3
			|	3	5
			|		8	
			|	2		// output: 2 8
	4		|			// output: 4 3
			|	4	4
			|		8
			|	2		// output: 2 8
			|			// output: 4 3

OUTPUT:	3 7
	5 3
	2 8
	4 3
	2 8
	4 3

   
   b. 
	public static double triangleArea(int base, int height) {
	    double area = (base * height) / 2.0; 
            return area;
        }

2. a. a and b can be printed because they are the only variables declared in the function so far.
   b. a, b, and i can be printed. a and b have been declared within the doSomething method and i has been declared in the for loop.
   c. a, b, i, c, and j can be printed because they have previously been declared in the method containing this println statement.
   d. only a, b can be printed because the scope of the i, c, j, and d have ended at the closing brace of the for loop that contains these variables.
   e. x can be printed because it is the only variable declared in the main method.


3. a. 	i. str1.substring(1, 4) = "lec"
       ii. str1.indexOf('l') = 1
      iii. str2.indexOf('!') = 21
       iv. str1.trim().substring(0, 5) = "Elect"
	v. str2.toUpperCase().substring(16) + str1.substring(9) = "COUNT!2008"

   b. 	i. str1.substring(9)
       ii. str2.toUpperCase().substring(6,9) + str2.substring(15)
      iii. str2.substring(11,15) + str2.substring(21)
       iv. str1.charAt(5)
	v. str1.lastIndexOf(" ")