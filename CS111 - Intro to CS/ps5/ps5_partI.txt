1. a. i. ruby
          pearl
          done

       ii. ruby
          pearl
          done

      iii. diamond
           pearl
           done

      iv. silver
          done

      v. penny
          nickle
          done

  b. "Dime" will never print because it will only print is a == b.
     However, the if a == b, the first if block will be executed.
     "Copper" will never print because its condition is a < b. If this is          true, the first if block will be executed and the else if block that
     contains this println statement will be skipped altogether.

2. a. public static double sumReciprocals(int n) {
         double sum = 0;
         for (double i = 1 ; i <= n; i++) {
           sum = sum + (1.0/i);
         }
         return sum;
      }

   b. public static void printCountdown(int n) {
         for (int i = n ; i >= 1 ; i--) {
           if (i != 1) {
             System.out.print(i + "...");
           }
           else {
           System.out.print(i);
           }
        }
      }

3. a. 4 iterations
   b. 4 iterations
   c. Loop does not run
   d. 1 iteration
   e. infinate loop